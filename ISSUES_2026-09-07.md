# 100 issues — westpeakwellness.com, 7 Sep 2026

Found by probing the live site, the built output, DNS, the dependency tree and the source. Each line says how it was found so it can be re-checked. Severity: **H** fix before it costs a client or a claim · **M** worth a session · **L** hygiene.

## A. Wrong or unverified facts on the page (the ones that matter most)

1. **H** Couples session priced at **$170** in two city×service FAQs (`lib/city-services.ts:67`) while Cliniko and `/pricing` say **$175**. A published fee that disagrees with the invoice.
2. **H** Couples intro says a **"120-minute extended option"** (`lib/services.ts:108`); Cliniko's Couples Extended is **110 minutes** (`lib/site.ts`, and `services.ts:139` says 110 two paragraphs later). Same page, two numbers.
3. **H** 75 pages show an "Updated" date that predates copy changed after it (`node scripts/review-dates.mjs`): today's 30-minute sweep and retitles edited body text without moving the date. The dated line is a claim to the reader and to Google, and it is currently false on 75 URLs.
4. **H** The EMDR page promises an "EMDR-trained Registered Clinical Counsellor" and the couples page "Gottman Method-informed" work — the counsellor whose training those describe is not taking new clients. Camille's own profile says "additional training in EMDR"; whether she is Gottman-trained is still an open question you have not answered. If she is not, the couples page describes a service nobody bookable delivers.
5. **H** `/services/punjabi-counselling` and six `/punjabi-counselling/<region>` pages, plus today's `/punjabi/regions`, offer Punjabi-language sessions. The only Punjabi-speaking counsellor is closed to new clients. A Punjabi speaker who books lands with Camille.
6. **M** Camille described as a "**native** Tagalog speaker" (`lib/practitioner-tl.ts`, `lib/practitioner-places-tl.ts`, and today's hub answer). Her onboarding says she speaks Tagalog; "native" is an inference.
7. **M** "Surrey has the largest Punjabi-speaking population of any city in Canada" (`lib/punjabi-regions.ts`, twice). By 2021 census mother-tongue counts Brampton is larger. Reword to "in BC" or cite the figure.
8. **M** "Psychotherapy becomes a regulated profession in 2027" stated as fact on three resource pages. The College's timeline has moved before; phrase as "scheduled for" with the source, or it becomes wrong the day it slips.
9. **M** EI sickness cap "$729 a week in 2026" hard-coded in eight places plus `components/Stat.tsx`. It changes every January; nothing flags it.
10. **M** "in 2026" ×7, "in 2027" ×6, "of 2025" ×1 in copy — each a sentence that goes stale on a date nobody is tracking.
11. **M** Org schema `medicalSpecialty: 'Psychiatric'` (`app/layout.tsx:124`). The practice is counselling; a psychiatrist is exactly what it says elsewhere it is not.
12. **M** EMDR hero: "Process painful memories so they stop running the show." Couples hero: "Strengthen communication, deepen connection, repair what's frayed." Both read as outcome promises under BCACC advertising standards, which the rest of the site is careful to avoid.
13. **M** Practitioners hub, TrustBar and footer say "Registered Clinical Counsellors · BCACC registered" for both; Camille's designation string is "RCC, CCC". True, but the practitioner page is the only place the CCC is explained.
14. **L** Founder's record has `bookable: true` and `acceptingNewClients: false` (`lib/practitioners.ts:62,66`). Two flags that mean opposite things to a reader of the code.
15. **L** `/reviews` exists with an H1 "Reviews and references" and 4,140 words explaining why there are none. Its title draws review-intent clicks to a page that cannot satisfy them.

## B. Search and crawl

16. **M** `robots.txt` disallows `/*/opengraph-image` for every agent including Googlebot. Google fetches `og:image` for Discover, rich results and link previews; a blocked image never appears. Allow it for Googlebot/Bingbot and keep the AI-agent block if that was the intent.
17. **M** 164 of 255 sitemap `lastmod` values are 2026-09-03 from one repo-wide edit; after today, a second cluster on 09-06/07. The field is losing the credibility the rest of the sitemap work earned.
18. **M** `/index` returns 200 with a canonical to `/` — fine for Google, but it is a duplicate URL that should 308 like `/guides/` does.
19. **M** Six Punjabi region pages declare `hreflang="pa"` → `/punjabi` (`app/punjabi-counselling/[region]/page.tsx:41`). That is not a translation of any of them; hreflang to a different page is the misuse the DECISIONS entry forbids.
20. **M** Camille's English place pages emit four `hrefLang="tl"` links and one `pa` — the `pa` is the footer's Punjabi link carrying an attribute it should not on a page with no Punjabi twin.
21. **M** Trauma queries (~500 impressions/month) still land on a city×service page at position 80+; the retitled EMDR page is the intended target but has no `/trauma` alias, no direct link from the city trauma pages' first paragraph, and the word "trauma" is not in its H1.
22. **M** `/online-counselling/vancouver` has 680 impressions at position 52 — the largest city in the province and the page's only differentiator is a language-access paragraph. No Vancouver-specific service, wait-time or public-intake facts.
23. **M** Search Console shows `/services/south-asian-mental-health` still receiving impressions (54) through a 308; the redirect target's title does not contain "South Asian", so the query that found it is orphaned.
24. **L** `/guides?utm_source=x` and any query string return 200 with the same canonical — correct, but nothing strips tracking parameters on the way in, so analytics sees them and the CDN caches each variant.
25. **L** `/blog/:slug` → `/guides/:slug` blindly, so `/blog/anything` produces a redirect into a 404 (a two-hop soft failure).
26. **L** No `humans.txt`, no `/.well-known/security.txt` (RFC 9116) — the second is what a researcher looks for before emailing `info@`.
27. **L** `apple-touch-icon.png` 404s at the root and no `<link rel="apple-touch-icon">` in the head; iOS "Add to Home Screen" gets a screenshot, not the logo.
28. **L** Web app manifest has `display: "browser"` and no `icons` array visible in the first 400 bytes — check it carries 192/512 icons or the manifest is decorative.
29. **L** Page titles on 45 city×service pages gained ", BC" today but the five longest pairs did not, so the pattern is inconsistent across one template.
30. **L** Bing holds zero pages, Brave four stale ones, DuckDuckGo one — Bing Webmaster Tools still unverified (`NEXT_PUBLIC_BING_VERIFICATION` unset).
31. **L** No Google Business Profile link, no directory listings (Psychology Today, BCACC directory, TherapyBC) — the off-site half of every ranking problem above.

## C. Performance and delivery

32. **M** HTML is served `Cache-Control: public, max-age=0, must-revalidate`: the browser cannot reuse a page it has already seen, and with the stylesheet now inlined every back-navigation re-downloads 48 KB gzipped.
33. **M** Inlining the CSS took HTML from 28 → 48 KB gzipped per page (measured live), 20 KB more than the estimate in DECISIONS.md (14 KB). The trade may still be right; the number in the decision is wrong.
34. **M** `_next/image` responses come back `max-age=0, must-revalidate` on a HEAD — verify the optimizer is actually caching at the edge; if not, every image is re-encoded per request.
35. **M** Homepage `<img>` `src` fallback requests the **3840 px** rendition; the srcset handles real browsers but any consumer that reads `src` (some previews, scrapers, older mail clients) pulls the 3840-wide file.
36. **M** `forest-path.jpg` uses `sizes="(max-width: 900px) 92vw, 70vw"` — the same over-declaration the hero had before today's fix; on a phone it fetches the 640 px file for a ~350 px slot.
37. **M** The Gurmukhi font is preloaded on the homepage, `/book` and every service page (`app/fonts-gurmukhi.ts` imported there) — 13 KB on every first visit for the footer's "ਪੰਜਾਬੀ" word. The file's own comment says the split was meant to stop exactly this.
38. **M** Share-card images render in ~2.1 s at the edge (measured) because each OG route decodes a 215 KB JPEG per cold start. Pre-resize the photo to 620×630 and embed that.
39. **M** LCP after today's inlining is unmeasured — PageSpeed's daily quota was exhausted before and after. The scorecard row was raised on the render-blocking flag alone.
40. **L** Three stylesheet files (75 + 15 + 5 KB) where one would do; Next splits per layout entry and each is fetched separately.
41. **L** `npm run build` output notes "2 route(s) prerendered to the framework error shell" (`/alberta`, `/ontario`): gated provinces exist as real routes rendering Next's bare 404 rather than the site's designed one.
42. **L** No `Cross-Origin-Opener-Policy` / `Cross-Origin-Resource-Policy` headers; low risk for this site but they are the two the security scanners now ask for.

## D. Security, privacy, mail

43. **H** `npm audit`: **Next 14.2.35 carries a high-severity DoS (RSC deserialization, and the image-optimizer remotePatterns issue)**. The fix range starts at 15.5.21 — a major upgrade that the whole gate suite will need to be re-run against.
44. **H** `next-auth` is pinned to **5.0.0-beta.32**. The client portal's authentication runs on a beta of a security library with no stable release to migrate to on this major.
45. **H** DMARC is `p=none`. Anyone can spoof `@westpeakwellness.com` to your clients and DMARC merely reports it. Move to `p=quarantine` once the two senders below align.
46. **H** SPF for the apex includes only Google. Resend sends via `send.westpeakwellness.com` (SPF `include:amazonses.com`) — check the From address on every transactional template uses that subdomain, or those mails are failing alignment.
47. **M** No DKIM record found at `resend._domainkey` — either Resend's key lives under a different selector or DKIM signing is not configured for the sending domain.
48. **M** No CAA record. Any CA can issue a certificate for the domain; one line fixes it.
49. **M** HSTS header carries `preload` but the domain's preload-list status is "unknown". Either submit it or drop the directive — a `preload` token that is not backed by submission is a promise not kept.
50. **M** CSP `script-src` includes `'unsafe-inline'`. With 26 JSON-LD blocks and inline hydration scripts that is hard to remove, but it neutralises the CSP against injected script. Nonces or hashes are the fix.
51. **M** Six of 26 JSON-LD `dangerouslySetInnerHTML` sites escape `<` before serialising; twenty do not. Content that ever contains `</script>` (a guide title, a FAQ answer) breaks the page.
52. **M** `/api/health` publicly returns the git commit hash. Harmless alone; it tells an attacker exactly which build to match against a CVE.
53. **M** Enquiry bodies, names and emails live in one growing JSON file in Vercel Blob (`inbound/messages.json`), retained 24 months by code. PIPA-compliant on paper; a single object with every enquiry ever is a wide blast radius for one leaked token.
54. **M** One-time codes for the portal allow 5 attempts per code but nothing limits how many codes one address can be sent; a hostile page can make the practice email a client fifty times.
55. **M** Private routes `/admin` and `/client-portal` rely on a 307 to `/signin` for privacy; they emit no `noindex` of their own, so any crawler that follows a session cookie sees an indexable page.
56. **L** `.env.example` documents seven variables nothing reads (`AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`, `AUTH_SECRET`, `NEXT_PUBLIC_FORM_ENDPOINT`, three profile URLs) and omits eight that are read (`NEXT_PUBLIC_PHONE`, `NEXT_PUBLIC_ALBERTA_LIVE`, `NEXT_PUBLIC_ONTARIO_LIVE`, `INLINE_CSS`, `SITE`, `SMOKE_PORT`, two Vercel ones).
57. **L** `eslint` 8.57 is end-of-life; `eslint-config-next` 14 will not move without the Next upgrade.
58. **L** `@types/node` 20 against an engine of `>=22.6.0`.
59. **L** Console logging in every cron route (`console.log` ×8) is the only observability; there is no structured log or alerting beyond the watchdog email.
60. **L** `wp-login.php` answers 403 (Vercel's firewall) but `/.env`, `/xmlrpc.php`-style probes get the full 50 KB designed 404 page — cheap to serve, but the WAF rule set is partial.

## E. Accessibility and UX

61. **M** The Cliniko booking `<iframe>` has a `title` but no fallback content or link for users whose browsers block third-party frames (CSP `frame-src` allows it; corporate proxies often do not).
62. **M** `/search?q=` returns 200 with no results markup when JavaScript is off; the page is `noindex, follow` so no harm to search, but a no-JS visitor gets a blank box.
63. **M** Six colour pairs sit within 0.25 of the WCAG AA threshold (`node scripts/contrast-audit.mjs`). One token nudge and the accessibility statement's claim is false.
64. **M** The hero photo on the homepage is the LCP element and is `fetchPriority="high"`, but the header logo SVG and three fonts are preloaded ahead of it in the head order.
65. **M** Footer renders "ਪੰਜਾਬੀ" via `site.languagesNative` without a `lang="pa"` span; screen readers pronounce Gurmukhi with English rules.
66. **M** 20 Tagalog twin pages are template-shaped at 815–875 words: same sections, city swapped. A Tagalog reader landing on two of them sees the same page.
67. **M** The Tagalog and the new Punjabi pages have never been read by a native speaker at the practice (32 + 1 pages of clinical copy).
68. **L** `/message-sent` (57-char description) and `/punjabi/sent` (70) fall below the 80-char floor the rest of the site holds; both are noindex, so only the share preview suffers.
69. **L** `/alberta` and `/ontario` have no `<h1>` — the gated-province shells.
70. **L** The stress-check and cost tools run entirely client-side; with JavaScript off they render their intro and nothing else, with no notice.
71. **L** The consent gate stores the analytics choice in `localStorage` — correct for privacy, but a user who declines on one device is counted on the next.
72. **L** Sticky "Book" bar covers the bottom 56 px of every page on mobile including the crisis-line paragraph on `/resources/bc-crisis-and-support-directory`.
73. **L** Related-link labels across ~80 data records still use the destination's title ("Burnout vs depression") rather than the reader's question; today's pass covered the homepage and a dozen records only.

## F. Content structure and freshness

74. **M** The founder's profile is 754 words and the thinnest English page; it is also the one page allowed to carry her name and registration number — the page a referrer or a journalist checks.
75. **M** `/practitioners` lists two people; the "How to choose" section added today speaks of "counsellors" generally. With one bookable counsellor the choice it describes does not exist yet.
76. **M** `/services/family-counselling` (1,241 words) is the only service page without a `whatItIs` diagram and with four FAQs to the others' six-plus.
77. **M** Kamloops queries (trauma, anxiety, couples, ACT, narrative therapy — ~90 impressions) all land on generic city×service pages; the city page names no Kamloops-specific public service.
78. **M** "Narrative therapy" is searched (Kamloops, 11 impressions) and appears nowhere on the site; there are six approach pages and this is not one.
79. **M** `/careers` and `/careers/registered-clinical-counsellor` earn 16 clicks/month — more than any client page — and the careers page does not say whether the practice is currently hiring.
80. **M** The homepage carries 90 of the site's 141 monthly clicks and its H1 is a tagline ("Counselling that meets you where you are."); the query that brings people is the brand name and the second is "online counselling BC", neither of which the H1 contains.
81. **M** 23 guides cite "26 weeks" of EI sickness benefits in 15 places; one source link per page, none dated — when Service Canada changes the figure there is no single place to update.
82. **L** The glossary answer paragraph added today promises entries for "rupture and repair" and "window of tolerance"; verify both exist as entries, not only as phrases in guides.
83. **L** `lib/guides-drafts.ts` cites `http://reproductivementalhealth.ca/` over plain HTTP — the only insecure external citation.
84. **L** Nine external citations refuse scripted requests (StatCan ×5, Vancouver.ca PDF, EMDRIA, BCBH, Sun Life). Fine today; the link-rot gate cannot tell you when one of them really dies.
85. **L** `/resources/verify-a-counsellor-in-bc` was retitled today toward "Registered Clinical Counsellor" but its `figure` is still the generic designations diagram; a screenshot of the BCACC register search would answer the query on the page.

## G. Operations and the practice

86. **H** A credential in `lib/practitioners.ts` expires **2026-12-31** (116 days) and one credential has no expiry recorded; **no professional-liability renewal date exists for either counsellor**, and Alberta claims rest on it.
87. **H** The 1:07 PM enquiry (id `a6f526bd`) in `/admin` is still unanswered while every page promises a reply "usually within one business day".
88. **H** `price-drift` (the gate that catches issue 1) only runs where `CLINIKO_API_KEY` is set. It is not set on this machine and the GitHub `CRON_SECRET` is not set either, so neither the drift gate nor the scheduled IndexNow run has ever executed from CI.
89. **M** Cron health expects `booking-mail` every 2 h and emails on double the interval; the Vercel Hobby plan runs crons at best-effort minute precision and may batch — the watchdog fires on a platform delay, not a failure, as the 6 Sep alert showed.
90. **M** `cliniko-sync` writes the catalogue every two hours; a Cliniko outage longer than the Blob's stale window leaves `/pricing` on the last-known figures with no visible "as of" time.
91. **M** Welcome emails to new Cliniko clients are on by default (`NEW_CLIENT_INVITES`) with a cap of 10 per run; a bulk import into Cliniko (a migration, a duplicate merge) would email ten strangers per cycle until it drains.
92. **M** Aman is hidden from Cliniko online bookings but still `bookable: true` in the site's data; if she is ever re-enabled in Cliniko the site starts routing to her without a code change — and the reverse is what you asked for.
93. **M** `/book` embeds Camille's calendar with `practitioner_id`; if she takes a week off the page shows an empty grid with no message, no alternative, and the enquiry form is one scroll below.
94. **M** Ten test files cover 102 library modules; none exercise an API route, the enquiry endpoint, the OTP flow or the welcome-email path end to end.
95. **M** `verify:ci` runs 27 gates and a build (~6 minutes locally); the smoke test opens its own server on port 4399 and the last run left one alive — the harness does not clean up on failure.
96. **L** 30 gate scripts and DECISIONS.md are the only documentation of how the site is meant to behave; there is no README section pointing a new developer at either.
97. **L** GSC data is exported by hand into `data/gsc/`; the next export is a reminder in a chat transcript, not a calendar entry or a cron.
98. **L** The IndexNow key file and key are public by design, but the weekly cron submits the whole sitemap regardless of what changed; IndexNow rate-limits repeat submissions of unchanged URLs.
99. **L** The Punjabi enquiry form on `/punjabi` and the region pages post to `/api/enquiry` with `source=/punjabi`; the admin view does not show source, so a Punjabi-language enquiry is indistinguishable from an English one when deciding who replies.
100. **L** No uptime or synthetic monitor exists outside the site's own cron watchdog — the watchdog runs on the platform it is watching.
